{
  "provider": {
    "google": {
      "__DEFAULT__": {
        "credentials": "../service_account.json",
        "project": "${var.learn-internal}",
        "region": "${var.region}"
      }
    }
  }
}